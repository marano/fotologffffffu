- @title = "#{@name} at fotologffffffu"

#fotologffffffu_tite{:style => 'text-align: left; padding: 10px;'}
    %a{:href => "/"}
        %h2
            fotologffffffu

%h1
    = "/#{@name}"

= partial('search')

= partial('share')

%div{:id => 'photos'}
    - @photos.each do |photo|
        - proxy_photo = camo(photo)
        %a(href = photo)
            %img(src = proxy_photo)

%p{:id => 'end'}
    that's alllllllll

